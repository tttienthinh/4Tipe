
\documentclass{article}

\usepackage{tikz}
\usetikzlibrary{calc,backgrounds}
\usepackage[tightpage]{preview}

\begin{document}

\begin{tikzpicture}[>=latex]

  %
  % Styles for states, and state edges
  %
  \tikzstyle{state} = [draw, very thick, fill=white, rectangle, minimum height=3em, minimum width=7em, node distance=8em, font={\sffamily\bfseries}]
  \tikzstyle{stateEdgePortion} = [black,thick];
  \tikzstyle{stateEdge} = [stateEdgePortion,->];
  \tikzstyle{edgeLabel} = [pos=0.5, text centered, font={\sffamily\small}];

  %
  % Position States
  %
  \node[state, name=model] {MODEL};
  \node[state, name=layer, below of=model, yshift=-8em] {LAYER};
  \node[state, name=loss, below of=model, right of=model, xshift=8em] {LOSS};
  %
  % Connect States via edges
  %
  \draw ($(model.south) + (-.5em,0)$) 
      edge[stateEdge] node[edgeLabel, xshift=-3em]{\emph{Contient une liste \\ de Layer}} 
      ($(layer.north) + (-.5em,0)$); 
  \draw ($(layer.north) + (.5em,0)$) 
      edge[stateEdge] node[edgeLabel, xshift=2em]{\emph{Close}} 
      ($(model.south) + (.5em,0)$);


\end{tikzpicture}

\end{document}
